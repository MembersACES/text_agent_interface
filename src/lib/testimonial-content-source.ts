/** Response header on GET /api/testimonials/solution-content. Body stays a bare array. */
export const TESTIMONIAL_CONTENT_SOURCE_HEADER = "X-Content-Source";

export type TestimonialContentSource = "backend" | "defaults";
